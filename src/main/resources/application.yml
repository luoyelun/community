github:
  client:
    id: eff356635bf6f6d97aba
    secret: 49cdadb9bca561c9bf315976579f722a319de898
  redirect:
    uri: http://localhost:8081/community/callback
spring:
  thymeleaf:
    #thymeleaf关闭缓存
    cache: false
  #数据库连接
  #h2
  datasource:
    url: jdbc:h2:D:\Works\Practice\Java\Web Project\Spring boot\community\db\community
    username: sa
    password: 123
    driver-class-name: org.h2.Driver
server:
  port: 8081
  servlet:
    context-path: /community
mybatis:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  type-aliases-package: com.community.model
  mapper-locations: classpath:mapper/*.xml
pagehelper:
  helper-dialect: h2 #数据库方言
  reasonable: true #分页合理化
  support-methods-arguments: true #是否支持接口参数来传递分页参数，默认false
  page-size-zero: false #当设置为true的时候，如果pagesize设置为0（或RowBounds的limit=0），就不执行分页，返回全部结果
